/**
 * Controller package — Presentation Tier.
 * 
 * Contains Spring MVC controllers that handle HTTP requests,
 * delegate to the service layer, and return views/responses.
 * 
 * Controllers in this package:
 *   - LoginController      : Handles user authentication endpoints
 *   - AppointmentController: Manages appointment CRUD operations
 *   - BillController       : Handles billing and receipt generation
 *   - HelpController       : Serves the help/documentation pages
 */
package com.sunrisedental.controller;
