/**
 * Config package — Application Configuration.
 * 
 * Contains Spring configuration classes for security,
 * database connections, and application-wide settings.
 * 
 * Configurations in this package:
 *   - SecurityConfig   : Spring Security configuration for authentication/authorization
 *   - WebConfig        : MVC and CORS configuration
 *   - DatabaseConfig   : DataSource and connection pool settings (Singleton pattern)
 */
package com.sunrisedental.config;
